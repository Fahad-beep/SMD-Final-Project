name: smd_final_project
description: Smart Travel Companion for the SMD final assignment.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_riverpod: ^2.4.9
  google_fonts: ^4.0.4
  go_router: ^12.1.3
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  http: ^0.13.6
  intl: ^0.18.1
  path_provider: ^2.1.2
  url_launcher: ^6.1.12

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3
  mocktail: ^1.0.3

flutter:
  uses-material-design: true
